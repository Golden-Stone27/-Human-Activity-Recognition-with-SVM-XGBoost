# -Human-Activity-Recognition-with-SVM-XGBoost
This project aims to classify **human activities** using **accelerometer-based time series data**.   The data is preprocessed, cleaned, and transformed into statistical features before being used to train and compare two models: **SVM** and **XGBoost**.
